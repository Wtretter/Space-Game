name: SpaceGame-Auto-Updater

on:
    push:
        branches: [ main ]

jobs:
    update:
        runs-on: self-hosted
        steps:
            - name: run-updater
              run: /home/ec2-user/update.sh